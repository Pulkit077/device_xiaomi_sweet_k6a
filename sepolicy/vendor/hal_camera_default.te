# Allow access to read /mnt/vendor/persist/camera/
r_dir_file(hal_camera_default, mnt_vendor_file)

get_prop(hal_camera_default, exported_camera_prop)

# Allow access to read gpu_available_frequencies
r_dir_file(hal_camera_default, vendor_sysfs_kgsl)

# Allow acces to read power_supply/bms/capacity
r_dir_file(hal_camera_default, vendor_sysfs_battery_supply)
